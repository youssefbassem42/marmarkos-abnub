# Runtime dependencies for the Vercel Python deployment.
# Mirrors pyproject.toml [project.dependencies] — keep both in sync.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7

sqlalchemy>=2.0
asyncpg>=0.30
alembic>=1.14

python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt>=4.2
python-multipart>=0.0.20

httpx>=0.28
segno>=1.6

email-validator>=2.2

# Serverless runtimes (Vercel) ship no OS zoneinfo database; without this
# wheel ZoneInfo("Africa/Cairo") raises and every attendance route 500s.
tzdata>=2024.1
